package ui;

import javax.swing.*;
        import java.awt.*;

public class PostgraduateCertificatePanel extends JPanel {

    public PostgraduateCertificatePanel() {
        // Panel setup
        setLayout(new BorderLayout());

        // Example content
        add(new JLabel("Postgraduate Certificate content goes here"), BorderLayout.CENTER);
    }
}